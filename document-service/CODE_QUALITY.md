# Руководство по Code Quality проверкам

## 📋 Оглавление

- [Что происходит при запуске `codeQuality`](#что-происходит-при-запуске-codequality)
- [Инструменты проверки](#инструменты-проверки)
- [Автоматическое исправление ошибок PMD](#автоматическое-исправление-ошибок-pmd)
- [Типы ошибок и их исправление](#типы-ошибок-и-их-исправление)
- [Команды Gradle](#команды-gradle)

---

## 🔍 Что происходит при запуске `codeQuality`

Когда вы запускаете `./gradlew allCodeQuality`, выполняется последовательность проверок:

### 1️⃣ **Компиляция кода**
```
compileJava → compileTestJava
```
- Проверяет, что код компилируется без ошибок
- Генерирует .class файлы для дальнейшего анализа

### 2️⃣ **Spotless (Форматирование кода)**
```
spotlessJavaCheck
```
**Проверяет:**
- Правильность форматирования кода
- Отступы (4 пробела)
- Удаление trailing whitespace
- Наличие newline в конце файла
- Google Java Code Style (AOSP вариант)

### 3️⃣ **PMD (Static Code Analysis)**
```
pmdMain → pmdTest
```
**Проверяет:**
- **Best Practices** (лучшие практики)
  - Неиспользуемые импорты
  - Неиспользуемые переменные
  - Правильное использование исключений
  
- **Code Style** (стиль кода)
  - Дублирование литералов
  - Длинные методы
  - Слишком много параметров
  - Правила именования

- **Design** (проектирование)
  - GodClass (классы с большой ответственностью)
  - Высокая связность (coupling)
  - TooManyFields

- **Error Prone** (склонность к ошибкам)
  - Пустые блоки catch
  - Неправильное использование equals()
  - Проблемы с многопоточностью

- **Performance** (производительность)
  - Неэффективное использование коллекций
  - Избыточное создание объектов
  - Использование устаревших API

**Генерирует отчеты:**
- `build/reports/pmd/main.html` - основной код
- `build/reports/pmd/test.html` - тестовый код
- XML версии для CI/CD

### 4️⃣ **CPD (Copy-Paste Detection)**
```
cpdCheck
```
**Проверяет:**
- Дублирование кода (более 100 токенов подряд)
- Похожие блоки кода в разных местах
- Кандидаты на рефакторинг

**Генерирует:**
- `build/reports/cpd/cpd-report.txt`

### 5️⃣ **SpotBugs (Bug Detection)**
```
spotbugsMain
```
**Проверяет:**
- Потенциальные баги и уязвимости
- Проблемы с null-значениями
- Проблемы синхронизации
- Утечки ресурсов
- SQL инъекции
- XSS уязвимости

**Генерирует:**
- `build/reports/spotbugs/main/spotbugs.html`

### 6️⃣ **Modernizer (Legacy API Check)**
```
modernizer
```
**Проверяет:**
- Использование устаревших API (legacy code)
- Рекомендует современные альтернативы
- Интегрировано с SpotBugs

---

## 🛠️ Инструменты проверки

### PMD 7.0
**Функции:**
- 300+ правил проверки кода
- Поддержка Java 21
- Встроенный CPD
- Расширяемая система правил

### SpotBugs 4.8.3
**Функции:**
- Наследник FindBugs
- 400+ детекторов багов
- Поддержка плагинов
- OWASP безопасность

### Spotless 6.25.0
**Функции:**
- Автоматическое форматирование
- Google Java Format
- Поддержка множества языков
- Интеграция с Git hooks

---

## 🔧 Автоматическое исправление ошибок PMD

### Какие ошибки можно исправить автоматически

#### ✅ **Автоматическое исправление:**

1. **UnnecessaryImport** - Удаление неиспользуемых импортов
```java
// До:
import java.util.List;
import java.util.ArrayList;
import java.util.HashMap;  // Не используется

// После:
import java.util.List;
import java.util.ArrayList;
```

2. **GuardLogStatement** - Добавление проверок логирования
```java
// До:
log.debug("Processing user: " + userId);

// После:
if (log.isDebugEnabled()) {
    log.debug("Processing user: " + userId);
}
```

3. **ControlStatementBraces** - Добавление фигурных скобок
```java
// До:
if (condition)
    doSomething();

// После:
if (condition) {
    doSomething();
}
```

4. **UseLocaleWithCaseConversions** - Добавление Locale
```java
// До:
String lower = text.toLowerCase();

// После:
String lower = text.toLowerCase(Locale.ROOT);
```

5. **UnnecessaryAnnotationValueElement** - Упрощение аннотаций
```java
// До:
@RequestMapping(value = "/api/users")

// После:
@RequestMapping("/api/users")
```

#### ⚠️ **Требуют ручного исправления:**

1. **AvoidDuplicateLiterals** - Извлечение констант
```java
// Проблема: одна строка используется 5+ раз
String message = "User not found";  // в 10 местах

// Решение: создать константу
private static final String USER_NOT_FOUND = "User not found";
```

2. **UseConcurrentHashMap** - Замена на потокобезопасные коллекции
```java
// Требует анализа: используется ли в многопоточном окружении?
Map<String, Object> map = new HashMap<>();  // Может быть проблема

// Решение (если нужно):
Map<String, Object> map = new ConcurrentHashMap<>();
```

3. **GodClass / TooManyFields** - Рефакторинг
```java
// Требует полного рефакторинга класса
// Разбить на несколько классов с отдельной ответственностью
```

4. **UnusedLocalVariable** - Удаление переменных
```java
// Требует анализа: может использоваться позже
int unused = calculate();  // Действительно не используется?
```

5. **JUnit naming conventions** - Исправление названий тестов
```java
// До:
@Test
public void createUser_GivenValidData_ShouldReturn201() { }

// После:
@Test
public void shouldReturn201WhenCreateUserWithValidData() { }
```

---

## 📝 Типы ошибок и их исправление

### Категория: Best Practices

| Правило | Описание | Автоисправление | Решение |
|---------|----------|-----------------|---------|
| `UnnecessaryImport` | Неиспользуемые импорты | ✅ Да | `pmdFix` |
| `UnusedLocalVariable` | Неиспользуемые переменные | ⚠️ Анализ | Ручное удаление |
| `AvoidCatchingGenericException` | Ловля общих исключений | ❌ Нет | Ловить конкретные |
| `PreserveStackTrace` | Потеря stack trace | ❌ Нет | Передавать cause |

### Категория: Code Style

| Правило | Описание | Автоисправление | Решение |
|---------|----------|-----------------|---------|
| `AvoidDuplicateLiterals` | Повторяющиеся литералы | ⚠️ Частично | Создать константы |
| `ControlStatementBraces` | Отсутствие фигурных скобок | ✅ Да | `pmdFix` |
| `MethodNamingConventions` | Неправильные имена | ⚠️ Частично | Переименовать |
| `UseExplicitTypes` | Использование var | ⚠️ Анализ | Указать тип явно |

### Категория: Performance

| Правило | Описание | Автоисправление | Решение |
|---------|----------|-----------------|---------|
| `UseConcurrentHashMap` | HashMap в многопоточном коде | ⚠️ Анализ | ConcurrentHashMap |
| `UseLocaleWithCaseConversions` | Нет Locale | ✅ Да | `pmdFix` |
| `AvoidFileStream` | Использование FileStream | ❌ Нет | Files.newInputStream |

### Категория: Design

| Правило | Описание | Автоисправление | Решение |
|---------|----------|-----------------|---------|
| `GodClass` | Класс-бог | ❌ Нет | Разбить на классы |
| `TooManyFields` | Слишком много полей | ❌ Нет | Рефакторинг |
| `CyclomaticComplexity` | Сложные методы | ❌ Нет | Упростить логику |

---

## 🚀 Команды Gradle

### Основные команды

```bash
# 1. Запустить все проверки качества (все сервисы)
./gradlew allCodeQuality

# 2. Запустить проверки для одного сервиса
./gradlew :document-service:codeQuality

# 3. Только PMD проверка
./gradlew pmdMain pmdTest

# 4. Только SpotBugs
./gradlew spotbugsMain

# 5. Только CPD (дублирование кода)
./gradlew cpdCheck
```

### Автоматическое исправление

```bash
# 1. Исправить код автоматически (PMD)
./gradlew :document-service:pmdFix

# 2. Исправить и проверить снова
./gradlew :document-service:pmdFixAndCheck

# 3. Форматирование кода (Spotless)
./gradlew spotlessApply

# 4. Полное исправление: форматирование + PMD
./gradlew spotlessApply :document-service:pmdFix
```

### Проверка результатов

```bash
# 1. Проверить форматирование
./gradlew spotlessCheck

# 2. Посмотреть отчеты PMD
# Откройте: build/reports/pmd/main.html

# 3. Посмотреть отчеты SpotBugs
# Откройте: build/reports/spotbugs/main/spotbugs.html
```

### Применить ко всем сервисам

```bash
# 1. Исправить форматирование везде
./gradlew allSpotlessApply

# 2. Исправить PMD ошибки во всех сервисах
./gradlew document-service:pmdFix \
          product-service:pmdFix \
          warehouse-service:pmdFix \
          organization-service:pmdFix \
          SSOService:pmdFix

# 3. Проверить все после исправлений
./gradlew allCodeQuality
```

---

## 📊 Интерпретация отчетов

### PMD Report

**Уровни серьезности:**
- 🔴 **High Priority** - Критические проблемы (исправить немедленно)
- 🟠 **Medium Priority** - Важные проблемы (исправить скоро)
- 🟡 **Low Priority** - Улучшения (исправить при возможности)

**Пример отчета:**
```
C:\...\UserService.java:45: AvoidDuplicateLiterals
  The String literal "User not found" appears 5 times
  
  Решение: 
  private static final String USER_NOT_FOUND_MSG = "User not found";
```

### SpotBugs Report

**Категории:**
- 🐛 **Correctness** - Логические ошибки
- 🔒 **Security** - Уязвимости безопасности
- ⚡ **Performance** - Проблемы производительности
- 💀 **Bad Practice** - Плохие практики

---

## 🎯 Workflow: Исправление ошибок

### Шаг 1: Запуск проверок
```bash
./gradlew allCodeQuality
```

### Шаг 2: Анализ отчетов
- Откройте `build/reports/pmd/main.html`
- Посмотрите список ошибок
- Определите, какие можно исправить автоматически

### Шаг 3: Автоматическое исправление
```bash
# Исправить форматирование
./gradlew spotlessApply

# Исправить PMD ошибки
./gradlew :document-service:pmdFix
```

### Шаг 4: Ручное исправление
Исправьте оставшиеся ошибки вручную:
- AvoidDuplicateLiterals → создать константы
- GodClass → рефакторинг классов
- UseConcurrentHashMap → анализ многопоточности

### Шаг 5: Повторная проверка
```bash
./gradlew allCodeQuality
```

### Шаг 6: Commit изменений
```bash
git add .
git commit -m "fix: resolve PMD violations"
```

---

## ⚙️ Настройка правил PMD

### Отключить конкретное правило

Отредактируйте `config/pmd/pmd-ruleset.xml`:

```xml
<rule ref="category/java/bestpractices.xml">
    <!-- Отключить правило -->
    <exclude name="GuardLogStatement"/>
</rule>
```

### Игнорировать в коде

```java
@SuppressWarnings("PMD.AvoidDuplicateLiterals")
public class UserService {
    // Код с дублированием литералов
}
```

---

## 📈 Метрики качества кода

### Целевые показатели:

| Метрика | Целевое значение | Инструмент |
|---------|------------------|------------|
| PMD violations | < 100 | PMD |
| Code coverage | > 85% | JaCoCo |
| Cyclomatic complexity | < 10 | PMD |
| Duplicate code | < 5% | CPD |
| SpotBugs issues | 0 high priority | SpotBugs |

---

## 🔄 CI/CD Integration

### GitHub Actions пример:

```yaml
name: Code Quality

on: [push, pull_request]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          
      - name: Run Code Quality Checks
        run: ./gradlew allCodeQuality
        
      - name: Upload PMD Report
        uses: actions/upload-artifact@v3
        with:
          name: pmd-report
          path: '**/build/reports/pmd/'
```

---

## 📚 Дополнительные ресурсы

- [PMD Rules Reference](https://pmd.github.io/pmd/pmd_rules_java.html)
- [SpotBugs Bug Descriptions](https://spotbugs.readthedocs.io/en/stable/bugDescriptions.html)
- [Google Java Style Guide](https://google.github.io/styleguide/javaguide.html)

---

## ❓ FAQ

**Q: Почему так много ошибок после первого запуска?**  
A: Это нормально. PMD проверяет множество правил. Начните с high priority ошибок.

**Q: Нужно ли исправлять все ошибки?**  
A: Нет, некоторые правила можно отключить если они не подходят вашему проекту.

**Q: Можно ли игнорировать ошибки в тестах?**  
A: Да, многие правила (например, JUnit naming) можно настроить или отключить.

**Q: Как часто запускать проверки?**  
A: Рекомендуется перед каждым commit или автоматически в CI/CD.

---

## 🎉 Заключение

Code Quality проверки помогают:
- ✅ Находить баги на ранней стадии
- ✅ Улучшать читаемость кода
- ✅ Поддерживать единый стиль
- ✅ Повышать производительность
- ✅ Обеспечивать безопасность

**Следуйте принципу:** 
> "Лучше исправлять автоматически, чем игнорировать!"

